json.array! @categories, :id, :name, :depth

# json.large_categories do
#   json.array! @large_categories, :id, :name, :depth
# end

# json.medium_categories do
#   json.array! @medium_categories, :id, :name, :depth
# end

# json.small_categories do
#   json.array! @small_categories, :id, :name, :depth
# end
